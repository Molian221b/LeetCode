编写一个函数来查找字符串数组中的最长公共前缀。
如果不存在公共前缀，返回空字符串 ""。

class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        word = str[0]
        for i in range(1,len(word)):
            count = 1
            while count < len(strs):
                if word[:i] == strs[count][:1]:
                    
